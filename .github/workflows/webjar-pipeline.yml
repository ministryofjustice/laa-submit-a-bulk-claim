name: Frontend dependency pipeline
on:
  workflow_dispatch:
  pull_request:

jobs:
  frontend-update:
    runs-on: ubuntu-latest

    steps:
      - name: Check gov.uk version
        run: npm view govuk-frontend version

